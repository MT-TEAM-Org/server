spring:
  profiles:
    active: prod
    include: auth, mail, swagger
  application:
    name: myteam-server

management:
  endpoints:
    web:
      exposure:
        include: "*"
  #        include: health, prometheus
  endpoint:
    prometheus:
      enabled: true
    env:
      enabled: true
    health:
      show-details: always
  security:
    enabled: false